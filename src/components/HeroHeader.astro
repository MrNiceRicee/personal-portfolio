<header class="hero bg-tranparent transition-colors duration-500">
  <div class="w-full h-full relative overflow-hidden">
    <div
      class="hero-radial-mask from-zinc-300 via-zinc-300 to-zinc-100
      dark:from-zinc-700 dark:to-zinc-900
      dark:mix-blend-lighten
      bg-gradient-to-b
      mix-blend-darken
      stop
      min-w-[800px] w-full h-[200%] bottom-[0%] left-[0%] origin-center"
    >
    </div>
    <div class="hero-image hidden dark:block bg-repeat"></div>
    <div class="hero-image-light dark:hidden"></div>
  </div>
  <div class="hero-inner-container">
    <div class="hero-inner" aria-details="header">
      <h1 class="text-7xl font-black ">joshua santos</h1>
      <p class="text-5xl font-extralight tracking-[.2em]">software engineer</p>
    </div>
  </div>
</header>

<style>
  .hero {
    height: calc(80vh - 96px);
    width: 100%;
    position: relative;
  }
  .hero-radial-mask {
    position: absolute;
    background-color: transparent;
    /* background-image: radial-gradient(
      circle,
      var(--tw-gradient-from) 0%,
      var(--tw-gradient-to) 60%
    ); */
  }
  .hero-linear-mask {
    position: absolute;
    background-color: transparent;
    /* background-image: linear-gradient(
      to bottom,
      var(--tw-gradient-from) 0%,
      var(--tw-gradient-to) 50%
    ); */
  }
  .hero-image {
    background-image: url('./endless-clouds.svg');
    background-color: transparent;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* background-image 112x56 */
    background-size: 84px 42px;
    /* two radial gradient circles */
  }
  .hero-image-light {
    background-image: url('./topography.svg');
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* background-image 112x56 */
    /* background-size: 84px 42px; */
    /* two radial gradient circles */
  }
  .hero-inner-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 80%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .hero-inner {
    position: sticky;
    top: 25%;
    text-align: center;
  }
</style>
